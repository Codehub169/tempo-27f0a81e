FLASK_APP=run.py
FLASK_ENV=development
# For production, set FLASK_ENV=production and use a strong, randomly generated SECRET_KEY.
SECRET_KEY='your_super_secret_key_for_flask_sessions_and_csrf'
JWT_SECRET_KEY='your_super_secret_key_for_jwt_signing'

# Database configuration (SQLite)
# The path is relative to the 'backend/instance' directory if using Flask's instance folder.
# If run.py is in 'backend/', and db is in 'backend/instance/clinic.db',
# then the path from where flask run is executed (backend/) would be 'instance/clinic.db'
# Flask-SQLAlchemy will create the 'instance' folder and 'clinic.db' file if they don't exist.
DATABASE_URL='sqlite:///instance/clinic.db'

# CORS settings
# For development, '*' allows all origins. For production, specify your frontend URL.
# Example: CORS_ORIGINS=http://localhost:3000,https://your-frontend-domain.com
CORS_ORIGINS='*'
