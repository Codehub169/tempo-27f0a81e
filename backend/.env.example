FLASK_APP=run.py
FLASK_ENV=development
# For production, set FLASK_ENV=production and use a strong, randomly generated SECRET_KEY.
SECRET_KEY='your_super_secret_key_for_flask_sessions_and_csrf'
JWT_SECRET_KEY='your_super_secret_key_for_jwt_signing'

# Database configuration (SQLite)
# For 'sqlite:///your_db_name.db', the database file 'your_db_name.db' will be located
# inside the Flask application's instance folder (e.g., 'backend/instance/your_db_name.db').
# The Flask app is configured with instance_relative_config=True.
DATABASE_URL='sqlite:///clinic.db'

# CORS settings
# For development, '*' allows all origins. For production, specify your frontend URL.
# Example: CORS_ORIGINS=http://localhost:3000,https://your-frontend-domain.com
CORS_ORIGINS='*'
